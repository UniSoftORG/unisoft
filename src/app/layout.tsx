/** @format */

import './globals.css'
import type { Metadata } from 'next'
import PrepareRenderer from '@/renderer/PrepareRenderer'
// import { generateElement } from '@/definitions/generators'
// import { KnownElementTag, Operators } from '@/types'
// import {
//   setState,
//   useEffect,
//   useFunction,
//   useInterval,
// } from '@/definitions/executors'
// import { FunctionNames } from '@/types/uniFunctions'
// import { ternaryCondition } from '@/definitions/evaluators'
// import { getState } from '@/definitions/getters'
import {NavWithLogo} from "@/predefined/components/Navigations/NavWithLogo";

export const metadata: Metadata = {
  title: 'UniSoft',
  description: 'Generated by create next app',
}

export default async function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {

  return (
    <html lang='en'>
      <body>
        {PrepareRenderer(NavWithLogo, 23123, true)}
        {children}
      </body>
    </html>
  )
}
